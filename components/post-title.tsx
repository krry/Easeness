const PostTitle = ({children}) => {
  return (
    <h1 className="mb-12 text-6xl text-center md:text-7xl lg:text-8xl md:text-left pretty-heading rainbow-wheel">
      {children}
    </h1>
  )
}

export default PostTitle
